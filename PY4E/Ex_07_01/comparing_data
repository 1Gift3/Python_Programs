# comparing e.g and count lines starting with from
fh = open('mbox-short.txt')
count= 0 
for line in fh:
    line = line.rstrip()
    if line.startswith('From:'):
        count += 1
print(f'There are {count} lines that start with "From:')        